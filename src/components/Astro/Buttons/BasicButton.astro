---
// Props
const {
    id,
    className
} = Astro.props;

---

<button
    class={`px-4 py-2 rounded-md font-bold text-white transition-colors bg-muted hover:bg-accent hover:cursor-pointer ${className}`}
    id={id}
>
    <slot />
</button>