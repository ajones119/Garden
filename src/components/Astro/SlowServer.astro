---
import { setTimeout as delay } from "timers/promises";
export const prerender = false;
// Simulate a slow API or computation
await delay(5000);

const timestamp = new Date().toLocaleTimeString();
---

<div style="border: 2px dashed tomato; padding: 1rem; font-family: sans-serif;">
  <h2>⏳ Deferred Server Component</h2>
  <p>This content was rendered on the server after the main page load.</p>
  <p><strong>Render time:</strong> {timestamp}</p>
  
</div>
